# 리팩토링·개선 백로그

> **상태: 진행 중인 작업 목록** — 2026-08-08 전수 감사(보안·성능·확장성·코드 품질)
> 결과에 2026-08-27 재점검(자체 + Codex 교차 리뷰)을 통합한 것. 완료 항목은
> ✅로 남겨 "왜 이 코드가 이런 모양인지"의 근거를 보존한다.
> 우선순위: 깨진 것 → 근본/작은 것 → 매일의 불편 → 확장.
>
> **줄 수·테스트 수 같은 수치는 이 문서에 적지 않는다.** 손으로 적으면 며칠 만에
> 낡는다 — 실제로 `app.rs`가 2449줄로 적혀 있는 동안 2644줄이 됐다.
> 필요할 때 `./scripts/code-metrics.sh` 로 현재 값을 본다.

## 0. 이번 감사에서 확인된 강점 (건드리지 말 것)

- IPC 인증: CSPRNG 토큰 + 상수시간 비교 + 64KB 상한 + 타임아웃. 토큰 파일 0600 생성.
- 명령 실행: `Command::new + args` — shell 경유 없음, 인젝션 표면 없음.
- 키 입력·검색어를 로그에 남기는 코드 없음 (키로거화 방지).
- 검색: Nucleo + 사전 소문자화 — 이미 성능 지향.
- 자체 코드에 TODO/FIXME 마커 0건 (전부 vendor/).
- 테스트: kmd-core 171 · kmd-daemon 71 · kmd-desktop 77.

## 1. 신뢰성·안전 ✅ 대부분 완료

| 항목 | 상태 |
|---|---|
| FFI 훅 콜백 패닉 격리 (catch_unwind, 양 플랫폼) | ✅ 1fdf627 |
| 탭 타임아웃 서킷 브레이커 (CPU 기아 시 키보드 잠금 방지) | ✅ 2026-08-08 사고 후 |
| 인덱스 캐시 0600 + 데이터 디렉터리 0700 | ✅ 223ed85 |
| status의 키 훅 진단 (조용한 훅 사망 가시화) | ✅ |
| macOS 서명·TCC: 전용 키체인 + launchd 재시작 | ✅ docs/07 §7 |
| **R1-1. Windows alt+space 무반응** | ✅ 원인 규명·수정 (2026-08-07, Windows 머신에서 직접) — 데몬은 살아 있는데 **OS가 LL 훅을 조용히 제거**(Modern Standby 복귀·콜백 타임아웃)해 키맵만 죽는 문제였다. 실측: 데몬 22시간 가동 중 Standby 복귀 20회+, 훅 로그 전무. 하트비트+워치독+재설치 3단으로 자동 복구 (25bf21c, main 병합됨). status에 훅 생존 표시 추가. 실사용 검증 대기 |
| **R1-2. spawn+토글 레이스 제거** | ✅ 2026-08-08 — 창 표시 마커(kmd.shown) 도입: 창이 실제로 뜬 인스턴스만 토글 대상. 부팅 중 재-핫키는 조용히 물러나고, 10s 유예를 넘긴 미표시 인스턴스는 멈춘 것으로 보고 회수 허용. 3개 백엔드 공통, E2E(표시→마커→토글→정리) 검증 |
| **R1-3. 콜드 스타트 실측·개선** | 🔶 웜 스타트 실측 706ms(spawn→창 표시, 2026-08-08). 진짜 콜드(재부팅 직후/하루 첫 실행)는 사용자 환경에서만 측정 가능 — 다음 아침 첫 alt+space가 느리면 그 직후 `kmd-data/desktop/logs/desktop.log`의 부팅 타이밍 로그 확인. R1-2 덕에 느려도 재-핫키가 창을 죽이지는 않음 |
| **R1-4. 데몬 .app 번들화 검토** | ⬜ TCC가 번들을 훨씬 안정적으로 다룸 — 접근성 재부여 마찰의 근본 해법 후보 |

## 2. 코드 품질 (2026-08-08 스윕에서 발견)

| 항목 | 내용 |
|---|---|
| **R2-1. clippy 정리** | ✅ 유지 중 — 경고 0건. 주의: `VKey::ALL`·`DelayedRefocus`는 죽은 코드가 아니라 **cfg(windows) 전용 코드의 사용처**였다(macOS 빌드에서만 미사용으로 보임) — 삭제 대신 `cfg_attr(not(windows), allow(dead_code))` 처리. 삭제했으면 Windows가 깨졌다 |
| **R2-2. 대형 파일** | 🔶 진행 중. **줄 수는 여기 적지 않는다** — 손으로 적으면 며칠 만에 낡는다(`app.rs`가 2449줄로 적혀 있는 동안 실제로는 2644줄이 됐다). 현재 값은 `./scripts/code-metrics.sh` 로 확인한다.<br>· `keybind/engine.rs`의 `process_key` 451줄 단일 함수는 **✅ 분해 완료** (2026-08-27) — 진입점 43줄 + 12개 스텝. `process_key_동작_스냅샷` characterization 테스트가 의미 보존을 지킨다.<br>· `kmd-desktop/app.rs`가 여전히 최대. `app/` 서브모듈 분리는 계속하되 `view_detail_panel`·`update_inner`가 다음 후보.<br>· `step_layer_mapping`(153줄)은 레이어 내 매핑이라는 하나의 관심사라 추가 분해는 필요해질 때. |
| **R2-3. CLI/TUI 테스트 공백** | ✅ 대부분 해결 (2026-08-28) — `src/` 테스트 4개 → **24개**. 프리픽스 디스패치 테이블, 선택 상태 전이, 설정 항목 왕복·거부, `execute_selected`의 상태 분기(도움말 시드 전환·bang 힌트·미지 명령→:help·설정 모달), 드릴다운 진입/복귀를 덮었다.<br>의도적 제외: 셸 실행·클립보드·브라우저 열기·config 저장처럼 **부작용이 상태 밖으로 나가는 분기**는 단위 테스트 대상이 아니다 |
| **R2-4. cargo-deb 한글 재발 방지** | ✅ 완료 (2026-08-28) — `scripts/check-encoding.sh` + CI 잡. UTF-8 유효성(CP949 혼입)과 BOM 규칙을 본다. **`.ps1`은 BOM이 필수**(PS 5.1이 없으면 ANSI로 읽어 한글을 깨뜨림), 나머지는 금지. 도입하자마자 `assemble-config.ps1`에 BOM이 빠져 있던 것을 잡았다. 대상 파일이 0개면 "통과"가 아니라 실패(exit 2)로 본다 — 조용한 통과가 가장 위험하다 |
| **R2-5. 설정 키 4중 열거** | ✅ 대부분 해결 (2026-08-28) — `config_registry!` 매크로가 **한 줄 선언에서 get/set 양쪽을 생성**한다. 단순 키 27개(숫자·불리언·문자열)는 비대칭이 원천적으로 불가능해졌고 `get_value`/`set_value`의 중복 arm 54개가 사라졌다. 리스트 정규화·읽기 전용 의사 키 등 16개는 여전히 명시적 arm(정당함). 테스트가 레지스트리↔명시적 arm 중복까지 막는다.<br>남은 것: **UI 목록(`items.rs`)은 여전히 별도 열거** — 라벨·위젯·설명이 붙어 있어 합칠 수 없고, 왕복 테스트가 누락을 막는다 |
| **R2-6. 프리픽스 라우팅 이중화** | ✅ 부분 완료 (2026-08-27) — 두 UI가 각자 벗겨내던 프리픽스 문자열(`:emoji`\|`:e`, `!`\|`>`, `:keymap`\|`:km`)을 `kmd_core::query_prefix`의 순수 함수로 모았다. **나머지는 의도적으로 통합하지 않았다** — `transform`(데스크톱=결과 항목/TUI=상태 메시지), `prompt`·`keys`(캐시된 config vs 매번 디스크 로드), `web`(점수 부여 상이)는 실제로 갈라진 동작이다. 통합하려면 먼저 어느 쪽이 옳은지 정해야 한다 |
| **R2-7. TUI가 검색마다 config를 디스크에서 읽는다** | ✅ 완료 (2026-08-28) — `AppState.config` 캐시를 두고 `:keys`/`:keymap`/`:prompt`/`?` 핸들러가 이를 쓴다. 설정을 바꾸는 경로(설정 모달 저장, 폴더 제안 추가, 키맵 액션, 템플릿 add/remove)는 캐시를 직접 고쳐 어긋나지 않게 했다. `load_config()` 호출이 11곳 → **1곳**(시작 시 1회)<br>⚠️ 잔여: 메인 루프가 소유한 `config`와 `AppState.config` **두 벌이 공존**한다. 저장 시 함께 갱신하지만 구조적으로는 한 벌이어야 맞다 — 메인 루프 시그니처를 건드려야 해 별도 작업으로 남긴다 |

## 3. 확장 구조 (P3 — 분리 구조 유지 결정 하에)

배경: 확장(클립보드 등)이 필요로 하는 자원(상주 상태·주입·이전 창)은 데몬에
있는데 확장은 UI에서 돈다. 상세 분석은 2026-08-08 세션 기록 참조.

| 항목 | 상태 |
|---|---|
| **R3-1. InjectPaste 능력** | ✅ E2E 검증 완료 (2026-08-08) — 데몬이 IPC로 전경 앱(TextEdit)에 Cmd+V 주입, 표식 문자열 붙여넣기 실증. 분리 구조에서 붙여넣기류 확장이 가능함이 증명됨 |
| **클립보드 P1 (docs/12 흐름 A)** | ✅ 2026-08-08 — 데몬 수집 + clip:N 즉시 붙여넣기, E2E 검증. R3-2 불필요(전경 앱에 이미 포커스). |
| **R3-2. 전경창 캡처** | ✅ 2026-08-08 — NSWorkspace 전경 PID 캡처/활성화, Launch 시점 캡처, E2E 검증(캡처→타앱→복귀 붙여넣기). docs/12 P2 |
| **클립보드 P1.1 프라이버시** | ✅ Concealed(비번) 제외 + changeCount 감시(macOS) |
| **클립보드 P3 런처 UI** | ✅ 2026-08-08 구현 — `;`/`:clip` 검색→Enter=이전 앱 붙여넣기. 라우팅·IPC 검증, GUI 상호작용만 실기기 확인 대상. docs/12 §6 |
| **R3-3. 삼중 중복 정리** | ⬜ calc/emoji/shell 확장과 SearchEngine 배선이 desktop·tui·daemon 3곳에 중복 — 단일 등록 지점으로 |
| **R3-4. ExtensionAction 확장** | ⬜ `Paste`/`Inject` 추가 + 순수형(클라이언트)/능력형(데몬 IPC) 경계 도입 |

## 4. 외부 대기

- **winget**: PR #413755 검증 통과, 모더레이터 머지 대기 → 머지 후 `WINGET_GITHUB_TOKEN` 등록 + 저장소 개명(`docs/07 §6` 런북)
- **Windows 창 문제**: R1-1 진단값 (사용자 Windows 머신)

## 권장 착수 순서 (2026-08-27 갱신)

1. **R2-2 `kmd-desktop/app.rs` 분해 계속** — `view_detail_panel`·`update_inner`가
   다음 후보. `process_key` 때처럼 characterization 테스트를 먼저 깔 것
4. **R2-3 테스트 공백 마저** — `execute_selected`의 실행 분기, 드릴다운 진입/복귀
5. R1-4 데몬 .app 번들화 / R3-3·R3-4 확장 구조 (설계 결정 필요)
6. R2-4 인코딩 검사 (CI 한 줄)

**보류 중** — 판단이 필요해 손대지 않은 것:
- `ipc::Response::status_lines`가 프로토콜 크레이트에 표시 문구를 갖고 있다
  (Codex 지적). 동작 결함은 아니고, 다음 출력 형식(JSON/영문)이나 소비자가
  생길 때 `StatusView` + presenter로 나누는 게 맞다
- 프리픽스 핸들러 중 갈라진 것들(R2-6) — 통합하려면 먼저 어느 동작이 옳은지
  정해야 한다
